"""opsd — on-policy context distillation from reference solutions.

A self-contained training folder that distills a student toward a teacher which
has seen a full *reference solution* for the problem (context distillation),
rather than the mistake-point hint the main `pipeline/` uses. See opsd/README.md.
"""
